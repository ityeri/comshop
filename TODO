* DOCS (in english)

* more convenient custom argument type define

* custom suggestion support for custom argument type (by flipping order of name and argument?: word() to "word")

* improve ArgumentNodeBuilderFactory. (by flipping order of name and argument?)

* completable future support (async support)

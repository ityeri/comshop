{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"comshop","text":"<p>comshop is a Kotlin DSL library for defining Minecraft commands on Paper servers, built on top of Brigadier.</p> <p>The goal is to let you write commands in a declarative, type-safe Kotlin DSL \u2014 without touching Brigadier nodes or version-specific Paper APIs directly:</p> <pre><code>register(\"sendtozero\") {\n    arguments {\n        \"entities\" named entities().asArg\n    }\n\n    executes {\n        val entities: List&lt;Entity&gt; = get(\"entities\")\n\n        entities.forEach {\n            it.teleport(Location(it.world, 0.0, 0.0, 0.0))\n        }\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"#why-comshop","title":"Why comshop?","text":"<p>Writing commands against Brigadier directly means hand-building a tree of literal and argument nodes, wiring <code>requires</code> predicates, executors, and suggestion providers, then registering the result \u2014 verbose, mutable, and tightly coupled to version-specific APIs:</p> <pre><code>// Brigadier (Paper API) style \u2014 the same \"sendtozero\" command\nLiteralCommandNode&lt;CommandSourceStack&gt; root = new LiteralCommandNode&lt;&gt;(\n    \"sendtozero\", null, ctx -&gt; true, null, null, false);\nArgumentCommandNode&lt;CommandSourceStack, EntitySelectorArgumentResolver&gt; entities =\n    new ArgumentCommandNode&lt;&gt;(\"entities\", ArgumentTypes.entities(),\n        null, ctx -&gt; true, null, null, false, null);\nroot.addChild(entities);\nentities.setCommand(ctx -&gt; {\n    for (Entity e : ctx.getArgument(\"entities\", EntitySelectorArgumentResolver.class)\n            .resolve(ctx.getSource())) {\n        e.teleport(new Location(e.getWorld(), 0.0, 0.0, 0.0));\n    }\n    return 1;\n});\nregistrar.register(root);\n</code></pre> <p>comshop compresses all of that into a handful of declarative blocks \u2014 the node wiring and the version-specific APIs disappear:</p> <ul> <li><code>requires { }</code> instead of requirement predicates</li> <li><code>arguments { \"name\" named type().asArg }</code> instead of manual argument node construction</li> <li><code>executes { }</code> with typed <code>get()</code> / <code>to</code> access instead of resolver casting and manual result codes</li> </ul>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li>Minimal boilerplate \u2014 a command that takes a dozen+ lines of raw Brigadier fits in a few DSL blocks</li> <li>Declarative DSL \u2014 <code>requires</code> / <code>arguments</code> / <code>executes</code> / <code>then</code> building blocks</li> <li>40 built-in argument types \u2014 primitives, strings, entities, positions, world state, and registry types</li> <li>Custom argument types \u2014 defined as converters on top of native types</li> <li>Custom suggestions \u2014 with tooltips and QUOTED-string handling</li> <li>Version-independent definitions \u2014 swap the implementation module to support a different Paper version</li> </ul>"},{"location":"#quick-links","title":"Quick links","text":"Page Description Installation Add comshop to your plugin via JitPack Quickstart Define your first command end to end Version Compatibility Which Paper versions are supported Argument Types Reference All built-in argument types Examples Complete copy-pasteable commands <p>comshop is distributed under the MIT License.</p>"},{"location":"examples/","title":"Examples","text":"<p>Complete, copy-pasteable commands. These are the same examples provided by the <code>example-plugin</code> module in the repository.</p>"},{"location":"examples/#mytp-overloads-optional-arguments","title":"mytp \u2014 overloads &amp; optional arguments","text":"<pre><code>register(\"mytp\") {\n    requires { sender.isOp }\n\n    arguments {\n        \"target\" named entity().asArg\n        \"to\" named entity().asArg\n    }\n    arguments {\n        \"target\" named entity().asArg\n    }\n\n    executes {\n        val target = \"target\" to Entity::class\n        val to = \"to\" toOrNull Entity::class\n\n        val from = if (to != null) {\n            target\n        } else {\n            if (entity != null) {\n                entity!!\n            } else {\n                sender.sendMessage(\"Only entity can use this command!\")\n                return@executes CommandResult.FAILED\n            }\n        }\n\n        val destination = to ?: target\n\n        from.teleport(destination.location)\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"examples/#sendtozero-list-argument","title":"sendtozero \u2014 list argument","text":"<pre><code>register(\"sendtozero\") {\n    arguments {\n        \"entities\" named entities().asArg\n    }\n\n    executes {\n        val entities: List&lt;Entity&gt; = get(\"entities\")\n\n        entities.forEach {\n            it.teleport(Location(it.world, 0.0, 0.0, 0.0))\n        }\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"examples/#fruit-enum-select-arguments","title":"fruit \u2014 enum &amp; select arguments","text":"<pre><code>enum class Fruit {\n    APPLE,\n    BANANA,\n    JAVASCRIPT,\n    STRAWBERRY\n}\n\n// enum-based argument\nfun FruitArgumentType() = enumArgument(Fruit::class)\n\n// selection-based argument (string)\nfun StringFruitArgumentType() = selectArgument(Fruit.entries.map { it.name })\n\nval fruitCommand = command(\"fruit\") {\n    arguments {\n        \"fruit\" named FruitArgumentType().asArg\n    }\n\n    executes {\n        val fruit = \"fruit\" to Fruit::class\n        sender.sendMessage(\"You are choose the ${fruit}!\")\n\n        CommandResult.SUCCESS\n    }\n}\n\nval stringFruitCommand = command(\"sfruit\") {\n    arguments {\n        \"fruit\" named StringFruitArgumentType().asArg\n    }\n\n    executes {\n        val fruit = \"fruit\" to String::class\n        sender.sendMessage(\"You are choose the ${fruit}!\")\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"examples/#myteam-custom-argument-subcommands","title":"myteam \u2014 custom argument &amp; subcommands","text":"<pre><code>val mm = MiniMessage.miniMessage()\n\nobject TeamManager {\n    val teams: MutableList&lt;Team&gt; = mutableListOf(\n        Team(\"asdf\", NamedTextColor.LIGHT_PURPLE, mutableListOf())\n    )\n\n    fun findByName(name: String): Team? =\n        teams.find({ it.name == name })\n}\n\nfun TeamArgumentType() = customArgument {\n    native(quotedString())\n\n    parses { nativeValue, source -&gt;\n        TeamManager.findByName(nativeValue)\n            ?: throw ComshopCommandException(\"The team name was not found\")\n    }\n\n    simpleSuggests(StringType.QUOTED) {\n        TeamManager.teams.map { it.name }\n    }\n}\n\ndata class Team(\n    val name: String,\n    val color: NamedTextColor,\n    val members: MutableList&lt;Player&gt;\n)\n\nval teamCommand = command(\"myteam\") {\n    requires { sender.isOp }\n\n    arguments {\n        \"team\" named TeamArgumentType().asArg\n    }\n\n    executes {\n        val team = \"team\" to Team::class\n        val teamColorHex = team.color.asHexString()\n\n        sender.sendMessage(\n            mm.deserialize(\"Information about the team \\\"&lt;$teamColorHex&gt;${team.name}&lt;/$teamColorHex&gt;\\\": \")\n        )\n        sender.sendMessage(\n            mm.deserialize(\"| ${team.members.size} players are joined to this team\")\n        )\n\n        CommandResult.SUCCESS\n    }\n\n    then(\"new\") {\n        arguments {\n            \"name\" named quotedString().asArg\n            \"color\" named namedColor().asArg\n        }\n\n        executes {\n            val existsTeam = TeamManager.findByName(\"name\" to String::class)\n\n            if (existsTeam != null) {\n                sender.sendMessage(mm.deserialize(\"&lt;red&gt;Team already exsists&lt;/red&gt;\"))\n                return@executes CommandResult.FAILED\n            }\n\n            val newTeam = Team(\n                \"name\" to String::class,\n                \"color\" to NamedTextColor::class,\n                mutableListOf()\n            )\n\n            TeamManager.teams.add(newTeam)\n\n            val teamColorHex = newTeam.color.asHexString()\n\n            sender.sendMessage(\n                mm.deserialize(\"Team \\\"&lt;$teamColorHex&gt;${newTeam.name}&lt;/$teamColorHex&gt;\\\" is added\")\n            )\n\n            CommandResult.SUCCESS\n        }\n    }\n\n    then(\"join\") {\n        arguments {\n            \"team\" named TeamArgumentType().asArg\n            \"player\" named player().asArg\n        }\n\n        executes {\n            val team = \"team\" to Team::class\n            val player = \"player\" to Player::class\n\n            team.members.add(player)\n\n            val teamColorHex = team.color.asHexString()\n\n            sender.sendMessage(\n                mm.deserialize(\n                    \"Player \\\"&lt;bold&gt;${player.name}&lt;/bold&gt;\\\" is added to team \" +\n                        \"\\\"&lt;$teamColorHex&gt;${team.name}&lt;/$teamColorHex&gt;\\\"\"\n                )\n            )\n\n            CommandResult.SUCCESS\n        }\n    }\n}\n</code></pre> <p>The commands above are registered in <code>onEnable</code>:</p> <pre><code>override fun onEnable() {\n    initComshop(this)\n\n    register(teamCommand)\n    register(fruitCommand)\n    register(stringFruitCommand)\n\n    register(\"mytp\") { ... }\n    register(\"sendtozero\") { ... }\n}\n</code></pre>"},{"location":"faq/","title":"FAQ","text":""},{"location":"faq/#i-used-requires-twice-only-the-last-one-works","title":"I used <code>requires</code> twice \u2014 only the last one works","text":"<p>By design. The <code>requires</code> block is overwritten by the last definition. See Command Structure.</p>"},{"location":"faq/#can-executes-return-other-integers","title":"Can <code>executes</code> return other integers?","text":"<p>No. Brigadier allows arbitrary integer results, but comshop currently only supports <code>0</code>/<code>1</code> via <code>CommandResult.SUCCESS</code> / <code>CommandResult.FAILED</code>. See Results &amp; Errors.</p>"},{"location":"faq/#why-do-i-need-two-dependencies-front-impl-","title":"Why do I need two dependencies (<code>front</code> + <code>impl-...</code>)?","text":"<p>The DSL (<code>front</code>) and the registration implementation (<code>impl</code>) are separate artifacts so that the same command code can run on multiple Paper versions. The <code>impl</code> module is loaded by reflection at runtime. See Installation.</p>"},{"location":"faq/#is-there-async-completablefuture-support","title":"Is there async / CompletableFuture support?","text":"<p>Not yet. Async command execution is on the roadmap.</p>"},{"location":"faq/#how-do-i-add-suggestions-to-a-custom-argument-type","title":"How do I add suggestions to a custom argument type?","text":"<p>Use the <code>suggests</code> block (or <code>simpleSuggests</code>) inside <code>customArgument { }</code>. A <code>suggests</code> block attached to an argument node overrides the type's own suggestions. See Custom Arguments.</p>"},{"location":"getting-started/compatibility/","title":"Version Compatibility","text":"<p>comshop separates the command definition (version-independent) from the registration implementation (version-specific). Your command code stays the same; only the <code>impl</code> artifact changes per Paper version.</p>"},{"location":"getting-started/compatibility/#supported-paper-versions","title":"Supported Paper versions","text":"Paper version Implementation module Compatibility 1.21.3 <code>impl-1.21.10</code> \u274c Incompatible 1.21.4 <code>impl-1.21.10</code> \u2705 Compatible 1.21.10 <code>impl-1.21.10</code> \u2705 Compatible 26.1.2 <code>impl-1.21.10</code> \u2705 Compatible 26.2 <code>impl-1.21.10</code> \u2705 Compatible <p>Notes:</p> <ul> <li>The <code>comshop-interface</code> module is compiled against the Paper 1.21.4 API.</li> <li>The only implementation module currently available is <code>impl-1.21.10</code>, built against Paper 1.21.10.</li> <li>The <code>example-plugin</code> test server runs Paper 26.2.</li> </ul>"},{"location":"getting-started/compatibility/#choosing-an-implementation-module","title":"Choosing an implementation module","text":"<p>Add the <code>impl-&lt;version&gt;</code> artifact that matches your server's Paper version. For example, on Paper 1.21.10:</p> <pre><code>dependencies {\n    implementation(\"com.github.ityeri.comshop:front:v2.1.0\")\n    implementation(\"com.github.ityeri.comshop:impl-1.21.10:v2.1.0\")\n}\n</code></pre>"},{"location":"getting-started/compatibility/#future-versions","title":"Future versions","text":"<ul> <li>Support for Paper versions below 1.21.4 is planned.</li> <li>New implementation modules will be published as separate artifacts (<code>impl-&lt;mc version&gt;</code>).</li> </ul> <p>If you want to contribute a new implementation module, see Adding a Version.</p>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>comshop is distributed via JitPack.</p>"},{"location":"getting-started/installation/#dependencies","title":"Dependencies","text":"<p>Add the JitPack repository and two dependencies to your plugin:</p>"},{"location":"getting-started/installation/#kotlin-dsl","title":"Kotlin DSL","text":"<pre><code>repositories {\n    maven { url = uri(\"https://jitpack.io\") }\n}\n\ndependencies {\n    implementation(\"com.github.ityeri.comshop:front:v2.1.0\")\n    implementation(\"com.github.ityeri.comshop:impl-1.21.10:v2.1.0\")\n}\n</code></pre>"},{"location":"getting-started/installation/#groovy","title":"Groovy","text":"<pre><code>repositories {\n    maven { url 'https://jitpack.io' }\n}\n\ndependencies {\n    implementation 'com.github.ityeri.comshop:front:v2.1.0'\n    implementation 'com.github.ityeri.comshop:impl-1.21.10:v2.1.0'\n}\n</code></pre> <p>Why two artifacts?</p> <p>comshop is split into two parts:</p> <ul> <li><code>front</code> \u2014 the command definition DSL and command structure</li> <li><code>impl-&lt;version&gt;</code> \u2014 the actual implementation that registers commands on a specific Paper version</li> </ul> <p>You always need both. The <code>impl</code> module is loaded at runtime via reflection; if it is missing, comshop throws a <code>ClassNotFoundException</code> with a helpful message.</p>"},{"location":"getting-started/installation/#initialize","title":"Initialize","text":"<p>Call <code>initComshop</code> with your plugin instance before the plugin is enabled:</p> <pre><code>import com.github.ityeri.comshop.initComshop\n\nclass MyPlugin : JavaPlugin() {\n\n    override fun onEnable() {\n        initComshop(this)\n    }\n}\n</code></pre> <p>Warning</p> <p><code>initComshop</code> must be called before the plugin is enabled. It registers the internal command registrar with Paper's lifecycle events, so commands defined after it will be picked up.</p>"},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Kotlin 2.x (comshop itself is compiled with Kotlin 2.3.0)</li> <li>JDK 21 or later (bytecode targets Java 21)</li> <li>Paper 1.21.4 or newer \u2014 see Version Compatibility</li> </ul>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This page walks through defining your first command end to end.</p>"},{"location":"getting-started/quickstart/#1-initialize-comshop","title":"1. Initialize comshop","text":"<p>In your plugin's <code>onEnable</code>, call <code>initComshop</code>:</p> <pre><code>class MyPlugin : JavaPlugin() {\n\n    override fun onEnable() {\n        initComshop(this)\n    }\n}\n</code></pre>"},{"location":"getting-started/quickstart/#2-register-your-first-command","title":"2. Register your first command","text":"<p>Use the top-level <code>register</code> function:</p> <pre><code>import com.github.ityeri.comshop.register\nimport com.github.ityeri.comshop.api.CommandResult\n\nregister(\"hello\") {\n    executes {\n        sender.sendMessage(\"Hello, world!\")\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"getting-started/quickstart/#3-add-arguments","title":"3. Add arguments","text":"<p>Arguments are declared with the <code>\"name\" named type().asArg</code> syntax inside an <code>arguments</code> block:</p> <pre><code>register(\"sendtozero\") {\n    arguments {\n        \"entities\" named entities().asArg\n    }\n\n    executes {\n        val entities: List&lt;Entity&gt; = get(\"entities\")\n\n        entities.forEach {\n            it.teleport(Location(it.world, 0.0, 0.0, 0.0))\n        }\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"getting-started/quickstart/#4-restrict-who-can-use-it","title":"4. Restrict who can use it","text":"<p>The <code>requires</code> block returns whether the sender is allowed to execute the command:</p> <pre><code>register(\"banme\") {\n    requires { sender.isOp }\n\n    executes {\n        sender.sendMessage(\"You are an operator!\")\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"getting-started/quickstart/#5-build-and-run","title":"5. Build and run","text":"<ul> <li>Add the dependencies from Installation.</li> <li>Build your plugin and drop the jar into your server's <code>plugins/</code> folder.</li> <li>The <code>example-plugin</code> module in the repository also provides a ready-to-run test setup (<code>gradle runServer</code>).</li> </ul>"},{"location":"getting-started/quickstart/#whats-next","title":"What's next","text":"<ul> <li>Command Structure \u2014 <code>requires</code>, <code>arguments</code>, <code>executes</code>, subcommands</li> <li>Defining Arguments \u2014 overloads and branching</li> <li>Custom Arguments \u2014 your own argument types</li> <li>Examples \u2014 complete commands</li> </ul>"},{"location":"guide/arguments/","title":"Defining Arguments","text":""},{"location":"guide/arguments/#the-named-asarg-syntax","title":"The named ... asArg syntax","text":"<p>Inside an <code>arguments</code> block, pair a name with an argument type using the <code>named</code> infix function and the <code>asArg</code> extension property:</p> <pre><code>register(\"somecommand\") {\n    arguments {\n        \"boolean\" named boolean().asArg\n        \"int\" named int(-100, 100).asArg\n        \"double\" named double().asArg\n        \"word\" named word().asArg\n        \"quotedString\" named quotedString().asArg\n        \"greedyString\" named greedyString().asArg\n    }\n}\n</code></pre> <p>Each line means: \"add an argument named <code>int</code> with an integer type\". The name is used later to read the value inside <code>executes</code> (see Reading Arguments).</p> <p>Note</p> <p>Do not confuse this with Kotlin's built-in <code>to</code> function. Earlier versions of comshop used <code>\"name\" to type()</code>; that syntax was removed in favor of <code>named ... asArg</code>.</p>"},{"location":"guide/arguments/#argument-options","title":"Argument options","text":"<p>A single argument can be customized before it is added:</p> <pre><code>arguments {\n    \"color\" named word()\n        .suggests {\n            suggest(\"red\")\n            suggest(\"green\")\n        }\n        .requires { sender.isOp }\n        .asArg\n}\n</code></pre> <ul> <li><code>requires { }</code> \u2014 per-argument permission check (runs with a <code>SourceContext</code> receiver)</li> <li><code>suggests { }</code> \u2014 custom suggestions, see Suggestions</li> </ul>"},{"location":"guide/arguments/#multiple-arguments-blocks-overloads","title":"Multiple arguments blocks: overloads","text":"<p>Writing several <code>arguments</code> blocks creates overloads. Each block becomes an alternative argument chain:</p> <pre><code>register(\"mytp\") {\n    arguments {\n        \"target\" named entity().asArg\n        \"to\" named entity().asArg\n    }\n    arguments {\n        \"target\" named entity().asArg\n    }\n\n    executes { ... }\n}\n</code></pre> <p>This makes both <code>/mytp &lt;target&gt; &lt;to&gt;</code> and <code>/mytp &lt;target&gt;</code> valid.</p>"},{"location":"guide/arguments/#branching-with-unions","title":"Branching with unions","text":"<p>For branching within a single argument chain, use <code>unions</code>. Every branch of a union is an alternative:</p> <pre><code>register(\"somecommand\") {\n    arguments {\n        \"int\" named int().asArg\n\n        unions {\n            \"boolean\" named boolean().asArg\n            \"double\" named double().asArg\n\n            arguments {\n                \"entity\" named entity().asArg\n                \"color\" named namedColor().asArg\n            }\n        }\n\n        \"word\" named word().asArg\n    }\n}\n</code></pre> <p>The command above can be used like:</p> <pre><code>/somecommand 10 false wasans\n/somecommand 10 3.14 wasans\n\n/somecommand 10 @s red wasans\n</code></pre>"},{"location":"guide/arguments/#adding-argument-structures-programmatically","title":"Adding argument structures programmatically","text":"<p>Besides the <code>arguments { }</code> block, a complete argument structure can be built and injected with <code>argument(builder)</code>:</p> <pre><code>import com.github.ityeri.comshop.builder.ArgumentStructureBuilder.ChainStructureBuilder\n\nval commonArguments = ChainStructureBuilder().apply {\n    \"x\" named int().asArg\n    \"y\" named double().asArg\n}\n\nregister(\"somecommand\") {\n    argument(commonArguments)\n\n    executes {\n        val x: Int = \"x\" to Int::class\n        ...\n        CommandResult.SUCCESS\n    }\n}\n</code></pre> <p><code>argument(builder)</code> takes any <code>ArgumentStructureBuilder</code> (single, chain, or union) and adds it to the command's argument alternatives. It is the programmatic counterpart of the <code>arguments { }</code> block \u2014 it is not a subcommand. Use <code>then(builder)</code> to add subcommands programmatically (see Command Structure).</p>"},{"location":"guide/arguments/#all-argument-types","title":"All argument types","text":"<p>See the Argument Types Reference for the complete list of built-in types.</p>"},{"location":"guide/command-structure/","title":"Command Structure","text":"<p>A comshop command is built from three building blocks \u2014 <code>requires</code>, <code>arguments</code>, <code>executes</code> \u2014 plus <code>then</code> for subcommands:</p> <pre><code>register(\"somecommand\") {\n    requires { ... }      // who can use the command\n    arguments { ... }     // what arguments it takes\n    executes { ... }      // what it does\n}\n</code></pre>"},{"location":"guide/command-structure/#register-vs-command","title":"register vs command","text":"<ul> <li><code>register(\"name\") { ... }</code> \u2014 defines the command and registers it.</li> <li><code>command(\"name\") { ... }</code> \u2014 only builds the definition and returns a <code>CommandBuilder</code>. Register it later:</li> </ul> <pre><code>val someCommand = command(\"somecommand\") {\n    requires { sender.isOp }\n}\n\n// later:\nregister(someCommand)\n</code></pre>"},{"location":"guide/command-structure/#requires","title":"requires","text":"<p>The <code>requires</code> block decides who is allowed to execute the command. It runs with a <code>SourceContext</code> receiver, so <code>sender</code>, <code>entity</code>, and <code>player</code> are available:</p> <pre><code>register(\"somecommand\") {\n    requires { sender.isOp }\n}\n</code></pre> <p>Only operators can use the command above. A sender check can be as complex as you want:</p> <pre><code>register(\"somecommand\") {\n    requires {\n        when (sender) {\n            is Entity -&gt; true\n            else -&gt; false\n        }\n    }\n}\n</code></pre> <p>If the <code>requires</code> block is not specified, all senders are allowed by default (server console, non-op players, and entities).</p> <p>Warning</p> <p>You cannot use the <code>requires</code> block more than twice. It will be overwritten by the last one.</p>"},{"location":"guide/command-structure/#arguments","title":"arguments","text":"<p>The <code>arguments</code> block declares the command's arguments. See Defining Arguments for the full syntax, overloads, and branching.</p>"},{"location":"guide/command-structure/#executes","title":"executes","text":"<p>The <code>executes</code> block is the command's behavior. It must return a <code>CommandResult</code> \u2014 see Results &amp; Errors for details:</p> <pre><code>register(\"somecommand\") {\n    arguments {\n        \"someInt\" named int().asArg\n    }\n\n    executes {\n        val intValue: Int = \"someInt\" to Int::class\n        sender.sendMessage(\"Value is $intValue\")\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"guide/command-structure/#subcommands-with-then","title":"Subcommands with then","text":"<p>Use <code>then</code> to create subcommands:</p> <pre><code>register(\"myteam\") {\n    then(\"new\") {\n        arguments { ... }\n        executes { ... }\n    }\n    then(\"join\") {\n        arguments { ... }\n        executes { ... }\n    }\n}\n</code></pre> <p>This supports commands like:</p> <pre><code>/myteam new sans-team red\n/myteam join sans-team ityeri\n</code></pre> <p>A <code>then</code> block accepts the same blocks as <code>register</code>/<code>command</code> \u2014 so subcommands of subcommands are possible. A subcommand can also be reused programmatically:</p> <pre><code>val newTeamCommand = command(\"new\") { ... }\n\nregister(\"myteam\") {\n    then(newTeamCommand)\n}\n</code></pre>"},{"location":"guide/command-structure/#command-overloads","title":"Command overloads","text":"<p>Using the <code>arguments</code> block several times creates a number of overloads for the command:</p> <pre><code>register(\"mytp\") {\n    arguments {\n        \"target\" named entity().asArg\n        \"to\" named entity().asArg\n    }\n    arguments {\n        \"target\" named entity().asArg\n    }\n\n    executes {\n        val target = \"target\" to Entity::class\n        val to = \"to\" toOrNull Entity::class\n\n        ...\n        CommandResult.SUCCESS\n    }\n}\n</code></pre>"},{"location":"guide/command-structure/#what-the-structure-looks-like-internally","title":"What the structure looks like internally","text":"<p>Each command becomes a tree:</p> <pre><code>literal (\"somecommand\")\n\u2514\u2500\u2500 union of branches\n    \u251c\u2500\u2500 subcommand 1 (literal \u2192 arguments \u2192 executes)\n    \u251c\u2500\u2500 subcommand 2\n    \u2514\u2500\u2500 default branch: arguments \u2192 executes\n</code></pre> <p>The DSL hides this completely \u2014 see Architecture if you are curious.</p>"},{"location":"guide/custom-arguments/","title":"Custom Arguments","text":"<p>A custom argument type is always built on top of a <code>NativeArgumentType</code>. It works like a converter: the native value is parsed first, then converted into your own type by custom logic.</p>"},{"location":"guide/custom-arguments/#the-interface","title":"The interface","text":"<p><code>ComshopCustomArgumentType&lt;T, N&gt;</code> has three members:</p> <ul> <li><code>nativeArgumentType: NativeArgumentType&lt;N&gt;</code> \u2014 the underlying native type</li> <li><code>parse(nativeValue: N, source: CommandSourceStack): T</code> \u2014 convert the native value into your type</li> <li><code>suggest(writingContext: CommandWritingContext, source: CommandSourceStack): Iterable&lt;SuggestionElement&gt;</code> \u2014 provide suggestions</li> </ul> <pre><code>class FruitArgumentType : ComshopCustomArgumentType&lt;Fruit, String&gt; {\n    override val nativeArgumentType: NativeArgumentType&lt;String&gt; =\n        NativeArgumentType.StringArgumentType(StringType.WORD)\n\n    override fun parse(nativeValue: String, source: CommandSourceStack): Fruit {\n        return Fruit.valueOf(nativeValue.uppercase())\n    }\n\n    override fun suggest(\n        writingContext: CommandWritingContext,\n        source: CommandSourceStack\n    ): Iterable&lt;SuggestionElement&gt; {\n        return Fruit.entries\n            .filter { it.name.lowercase().startsWith(writingContext.remainingLower) }\n            .map { SuggestionElement(it.name.lowercase()) }\n    }\n}\n</code></pre> <p>Use it like any other argument type:</p> <pre><code>arguments {\n    \"fruit\" named FruitArgumentType().asArg\n}\n</code></pre>"},{"location":"guide/custom-arguments/#the-builder-dsl","title":"The builder DSL","text":"<p>For most cases, <code>customArgument</code> is more convenient than implementing the interface:</p> <pre><code>fun TeamArgumentType() = customArgument {\n    native(quotedString())\n\n    parses { nativeValue, source -&gt;\n        TeamManager.findByName(nativeValue)\n            ?: throw ComshopCommandException(\"The team name was not found\")\n    }\n\n    simpleSuggests(StringType.QUOTED) {\n        TeamManager.teams.map { it.name }\n    }\n}\n</code></pre> <p>The builder requires three blocks \u2014 missing ones throw an <code>IllegalStateException</code> with a hint:</p> <ul> <li><code>native(...)</code> \u2014 the underlying native argument type (all factory functions from Argument Types are available)</li> <li><code>parses { nativeValue, source -&gt; ... }</code> \u2014 conversion logic</li> <li><code>suggests { ... }</code> \u2014 suggestion logic (or use <code>simpleSuggests</code>)</li> </ul>"},{"location":"guide/custom-arguments/#convenience-functions","title":"Convenience functions","text":""},{"location":"guide/custom-arguments/#selectargument-pick-from-a-list","title":"selectArgument \u2014 pick from a list","text":"<p>Creates a string-based argument that must match one of the given elements (case-insensitive by default):</p> <pre><code>fun StringFruitArgumentType() = selectArgument(Fruit.entries.map { it.name })\n</code></pre> <p>Overloads accept a provider lambda or varargs:</p> <pre><code>selectArgument { getAvailableNames() }            // dynamic list\nselectArgument(\"red\", \"green\", \"blue\")            // fixed list\n</code></pre> <p>Options:</p> Parameter Default Meaning <code>stringType</code> <code>WORD</code> Underlying string type <code>ignoreCase</code> <code>true</code> Case-insensitive matching <code>suggestOnlyMatches</code> <code>true</code> Only suggest matching elements <code>whenException</code> throws Custom error when the input does not match (<code>ComshopCommandException(\"Value \\\"...\\\" does not exist.\")</code>)"},{"location":"guide/custom-arguments/#enumargument-enum-as-argument","title":"enumArgument \u2014 enum as argument","text":"<p>Turns any enum into an argument type:</p> <pre><code>fun FruitArgumentType() = enumArgument(Fruit::class)\n</code></pre> <p>The <code>lowercase</code> parameter (default <code>true</code>) controls whether enum names are parsed and suggested in lowercase.</p>"},{"location":"guide/custom-arguments/#simplesuggests-common-suggestion-patterns","title":"simpleSuggests \u2014 common suggestion patterns","text":"<p>An extension on <code>CustomArgumentTypeBuilder</code> for the most common suggestion setups:</p> <pre><code>simpleSuggests(\n    stringType = StringType.QUOTED,\n    ignoreCase = false,\n    displayOnlyMatches = true\n) {\n    listOf(\"red\", \"green\", \"blue\")\n}\n</code></pre> <ul> <li><code>stringType</code> \u2014 when <code>QUOTED</code>, suggestions are wrapped in quotes and matching strips the quotes</li> <li><code>ignoreCase</code> \u2014 case-insensitive prefix matching</li> <li><code>displayOnlyMatches</code> \u2014 filter suggestions by the current input</li> </ul>"},{"location":"guide/custom-arguments/#error-handling-in-parse","title":"Error handling in parse","text":"<p>If parsing fails, throw <code>ComshopCommandException</code> \u2014 the message is shown to the sender:</p> <pre><code>parses { nativeValue, source -&gt;\n    val fruit = Fruit.entries.find { it.name.equals(nativeValue, ignoreCase = true) }\n        ?: throw ComshopCommandException(\"Fruit name is wrong!\")\n    fruit\n}\n</code></pre> <p>Brigadier's <code>CommandSyntaxException</code> is not allowed inside custom argument logic \u2014 throwing it is treated as a programming error (see Results &amp; Errors).</p>"},{"location":"guide/reading-arguments/","title":"Reading Arguments","text":""},{"location":"guide/reading-arguments/#sender-related-values","title":"Sender-related values","text":"<p>Inside <code>requires</code>, <code>executes</code>, and <code>suggests</code> blocks you have a <code>SourceContext</code> receiver:</p> Property Type Meaning <code>sender</code> <code>CommandSender</code> The command sender (console, player, or other entity) <code>entity</code> <code>Entity?</code> The executing entity (<code>source.executor</code>) <code>player</code> <code>Player?</code> The sender as a player, or <code>null</code> <pre><code>executes {\n    val sender: CommandSender = sender\n    val player: Player? = player\n    val entity: Entity? = entity\n    ...\n}\n</code></pre>"},{"location":"guide/reading-arguments/#reading-argument-values","title":"Reading argument values","text":"<p>Inside <code>executes</code>, four functions read argument values:</p> Function Returns Behavior on missing argument / type mismatch <code>\"name\" to KClass</code> (infix) <code>T</code> throws <code>IllegalArgumentException</code> <code>\"name\" toOrNull KClass</code> (infix) <code>T?</code> returns <code>null</code> <code>get&lt;T&gt;(\"name\")</code> (reified) <code>T</code> throws <code>IllegalArgumentException</code> <code>getOrNull&lt;T&gt;(\"name\")</code> (reified) <code>T?</code> returns <code>null</code> <pre><code>register(\"somecommand\") {\n    arguments {\n        \"someInt\" named int().asArg\n    }\n\n    executes {\n        val intValue: Int = \"someInt\" to Int::class\n        val nullableInt: Int? = \"someInt\" toOrNull Int::class\n\n        val intValue1: Int = get(\"someInt\")\n        val nullableInt1: Int? = getOrNull(\"someInt\")\n\n        CommandResult.SUCCESS\n    }\n}\n</code></pre> <p>Tip</p> <p>The type you read with must match the argument type's generic type. For example, <code>entities()</code> maps to <code>List&lt;Entity&gt;</code>:</p> <pre><code>arguments {\n    \"entities\" named entities().asArg\n}\n\nexecutes {\n    val entities: List&lt;Entity&gt; = get(\"entities\")\n    ...\n}\n</code></pre>"},{"location":"guide/results-and-errors/","title":"Results &amp; Errors","text":""},{"location":"guide/results-and-errors/#commandresult","title":"CommandResult","text":"<p>Every <code>executes</code> block must return a <code>CommandResult</code>:</p> <ul> <li><code>CommandResult.SUCCESS</code> \u2014 the command succeeded (mapped to brigadier result <code>1</code>)</li> <li><code>CommandResult.FAILED</code> \u2014 the command failed (mapped to brigadier result <code>0</code>)</li> </ul> <pre><code>executes {\n    ...\n    CommandResult.SUCCESS\n}\n</code></pre> <p>Note</p> <p>Brigadier allows returning any integer from a command, but comshop currently only supports <code>0</code> and <code>1</code> via <code>CommandResult</code>.</p> <p>The result is different from an execution error: a failed result does not show an error message. For example, the vanilla command</p> <pre><code>/execute as @e[type=minecraft:villager] at @s run kill @s\n</code></pre> <p>returns <code>1</code> when any villager exists, and <code>0</code> when none do. Use <code>CommandResult.FAILED</code> for the latter case, and exceptions for error messages.</p>"},{"location":"guide/results-and-errors/#comshopcommandexception","title":"ComshopCommandException","text":"<p>For command execution failures that should show a message to the sender, throw <code>ComshopCommandException</code>:</p> <pre><code>import com.github.ityeri.comshop.api.exception.ComshopCommandException\n\nexecutes {\n    val playerName = \"playerName\" to String::class\n\n    if (!playerList.contains(playerName)) {\n        throw ComshopCommandException(\"Target player does not exist!\")\n    }\n\n    CommandResult.SUCCESS\n}\n</code></pre> <p>The exception message is shown directly to the sender.</p> <p>The same rule applies inside custom argument parsing (<code>parses</code> blocks and <code>ComshopCustomArgumentType.parse</code>) \u2014 see Custom Arguments.</p>"},{"location":"guide/results-and-errors/#what-not-to-throw","title":"What not to throw","text":"<p>Brigadier's <code>CommandSyntaxException</code> belongs to the internal implementation. Throwing it from comshop blocks is treated as a programming error and re-wrapped in an <code>IllegalStateException</code> with an explanatory message:</p> <p>\"Command execution block defined in comshop should not throw CommandSyntaxException, which belongs to brigadier. Use ComshopCommandException instead\"</p> <p>Always use <code>ComshopCommandException</code> for user-facing errors.</p>"},{"location":"guide/suggestions/","title":"Suggestions","text":""},{"location":"guide/suggestions/#custom-suggestions-on-arguments","title":"Custom suggestions on arguments","text":"<p>Attach a <code>suggests</code> block to any argument to control what the client shows while typing:</p> <pre><code>register(\"somecommand\") {\n    requires { sender.isOp }\n\n    arguments {\n        \"color\" named word()\n            .suggests {\n                suggest(\"red\")\n                suggest(\"green\")\n                suggest(\"blue\")\n                suggest(\"white\")\n            }\n            .asArg\n    }\n}\n</code></pre>"},{"location":"guide/suggestions/#the-suggestion-builder","title":"The suggestion builder","text":"<p>Inside <code>suggests { }</code> you have:</p> <ul> <li><code>suggest(text: String)</code> / <code>suggest(text: String, tooltipMessage: Component?)</code> \u2014 add a suggestion, optionally with a tooltip</li> <li><code>sender</code>, <code>entity</code>, <code>player</code> \u2014 the sender-related values (from <code>SourceContext</code>)</li> <li><code>context: CommandWritingContext</code> \u2014 the current input state:</li> </ul> Property Meaning <code>fullInput</code> The full input string <code>start</code> Where the current argument starts <code>remaining</code> The remaining (unparsed) input \u2014 <code>fullInput.substring(start)</code> <code>remainingLower</code> <code>remaining</code> in lowercase <p>A tooltip example:</p> <pre><code>arguments {\n    \"color\" named word()\n        .suggests {\n            suggest(\"red\", Component.text(\"A warm color\"))\n            suggest(\"green\", Component.text(\"A calm color\"))\n        }\n        .asArg\n}\n</code></pre>"},{"location":"guide/suggestions/#suggestions-for-custom-argument-types","title":"Suggestions for custom argument types","text":"<p>Custom argument types define their own suggestions through the <code>suggests</code> block of <code>customArgument</code> (or via <code>simpleSuggests</code>). See Custom Arguments.</p>"},{"location":"guide/suggestions/#quoted-strings","title":"QUOTED strings","text":"<p>When the underlying string type is <code>quotedString()</code>, suggestions usually need quotes around them. The <code>simpleSuggests</code> helper handles this automatically: it wraps suggestions in quotes and strips quotes when matching against the current input.</p>"},{"location":"internals/adding-a-version/","title":"Adding a Version","text":"<p>comshop supports multiple Paper versions by publishing one <code>impl</code> module per version. This page explains how to add a new one, using the existing <code>comshop-impl/1.21.10</code> module as a template.</p>"},{"location":"internals/adding-a-version/#steps","title":"Steps","text":""},{"location":"internals/adding-a-version/#1-create-the-module","title":"1. Create the module","text":"<p>Copy the structure of <code>comshop-impl/1.21.10</code>:</p> <pre><code>comshop-impl/\n\u2514\u2500\u2500 &lt;mc version&gt;/                  e.g. comshop-impl/1.21.4\n    \u251c\u2500\u2500 build.gradle.kts\n    \u2514\u2500\u2500 src/main/kotlin/com/github/ityeri/comshop/impl/...\n</code></pre>"},{"location":"internals/adding-a-version/#2-register-it-in-the-build","title":"2. Register it in the build","text":"<p><code>settings.gradle.kts</code>:</p> <pre><code>include(\"comshop-impl\")\ninclude(\"comshop-impl:1.21.10\")   // \u2190 add your version here, e.g. \"comshop-impl:1.21.4\"\n</code></pre>"},{"location":"internals/adding-a-version/#3-declare-the-paper-api-dependency","title":"3. Declare the Paper API dependency","text":"<p><code>comshop-impl/&lt;version&gt;/build.gradle.kts</code>:</p> <pre><code>dependencies {\n    api(\"io.papermc.paper:paper-api:&lt;version&gt;-R0.1-SNAPSHOT\")\n    compileOnly(\"io.papermc.paper:paper-api:&lt;version&gt;-R0.1-SNAPSHOT\")\n}\n</code></pre> <p>The artifact name (<code>impl-&lt;version&gt;</code>) is derived automatically from the module path by the root publishing configuration.</p>"},{"location":"internals/adding-a-version/#4-implement-the-registrar","title":"4. Implement the registrar","text":"<p>Implement <code>AbstractCommandRegistrar</code> (from <code>comshop-interface</code>):</p> <ul> <li><code>init(plugin)</code> \u2014 register a <code>LifecycleEvents.COMMANDS</code> handler that converts and registers all collected nodes</li> <li><code>register(node)</code> \u2014 collect command definition nodes</li> </ul>"},{"location":"internals/adding-a-version/#5-convert-nodes-to-brigadier","title":"5. Convert nodes to Brigadier","text":"<p>Reuse the conversion pipeline from <code>comshop-impl/1.21.10</code>:</p> <ul> <li><code>toFinalBuilderBoundary</code> \u2014 one optics traversal maps <code>Node&lt;ComshopCommandNode&gt;</code> \u2192 <code>BrigadierBuilderBoundary</code> via <code>toBrigadierFragment</code> / <code>toBrigadierBuilderBoundary</code> and connects them</li> <li>the final <code>BrigadierBuilderBoundary</code> reduces to the Brigadier <code>LiteralCommandNode</code> root</li> <li><code>nativeTypeToBrigadierArgumentType</code> \u2014 the mapping between comshop's <code>NativeArgumentType</code>s and the target version's <code>ArgumentTypes</code> is the part most likely to differ between versions, so check each type against the target Paper API</li> </ul>"},{"location":"internals/adding-a-version/#6-test","title":"6. Test","text":"<ul> <li>Point the <code>example-plugin</code> at the new module (change <code>implementation(project(\":comshop-impl:...\"))</code> and the Paper version) and run the test server.</li> <li>Verify the full feature set: arguments, overloads, unions, subcommands, custom arguments, and suggestions.</li> </ul>"},{"location":"internals/adding-a-version/#7-update-compatibility-info","title":"7. Update compatibility info","text":"<ul> <li>The Version Compatibility page</li> <li>The compatibility table in the repository README</li> </ul>"},{"location":"internals/adding-a-version/#what-can-change-between-versions","title":"What can change between versions","text":"<ul> <li><code>ArgumentTypes</code> factory names and signatures (Paper's <code>io.papermc.paper.command.brigadier.argument.ArgumentTypes</code>)</li> <li>Resolver types for selector/position arguments</li> <li>Lifecycle event APIs</li> </ul> <p>The command DSL (<code>comshop-front</code>) and the node model (<code>comshop-interface</code>) should stay unchanged across versions.</p>"},{"location":"internals/architecture/","title":"Architecture","text":"<p>This page describes comshop's internal structure. You only need this if you are contributing to comshop or curious about how it works.</p>"},{"location":"internals/architecture/#module-layout","title":"Module layout","text":"<p>comshop is split into three core modules to support multiple Minecraft (Paper) versions:</p> Module Artifact Responsibility <code>comshop-interface</code> <code>interface</code> Version-independent API: node model, argument types, contexts, the <code>AbstractCommandRegistrar</code> SPI <code>comshop-front</code> <code>front</code> The user-facing DSL: <code>register</code>/<code>command</code>, builders, argument factories <code>comshop-impl:&lt;version&gt;</code> <code>impl-&lt;version&gt;</code> Version-specific implementation: converts comshop nodes into Brigadier nodes and registers them <p>Users depend on <code>front</code> + one <code>impl</code> module. <code>front</code> never links against <code>impl</code> at compile time \u2014 the implementation is loaded via reflection (see below).</p>"},{"location":"internals/architecture/#the-node-model","title":"The node model","text":"<p>All command definitions are built as trees of two sealed hierarchies:</p> <pre><code>sealed class Node&lt;out T&gt; {\n    class SingleNode&lt;T&gt;(val value: T) : Node&lt;T&gt;()\n    class UnionNode&lt;T&gt;(val nodes: Iterable&lt;Node&lt;T&gt;&gt;) : Node&lt;T&gt;()          // branching\n    class ChainNode&lt;T&gt;(val nodes: Iterable&lt;Node&lt;T&gt;&gt;) : Node&lt;T&gt;()          // sequencing\n}\n\nsealed class ComshopCommandNode {\n    class LiteralCommandNode(val name: String, val requiresChecker: ...)\n    class ArgumentNode&lt;T : Any&gt;(val name: String, val argumentType: ComshopArgumentType&lt;T&gt;, ...)\n    class ExecutionNode(val commandBlock: (ComshopContext) -&gt; CommandResult)\n}\n</code></pre> <p>A command built by <code>CommandBuilder.build()</code> has this shape:</p> <pre><code>ChainNode(\n    SingleNode(LiteralCommandNode),          // the command's literal\n    UnionNode(                                // branches\n        subcommand trees...,\n        ChainNode(                             // default branch\n            UnionNode(argument nodes...),      // the arguments\n            SingleNode(ExecutionNode)\n        )\n    )\n)\n</code></pre> <p>When a command has no arguments, the default branch is a plain <code>SingleNode(ExecutionNode)</code> (no argument <code>ChainNode</code> wrapper).</p>"},{"location":"internals/architecture/#argument-types","title":"Argument types","text":"<ul> <li><code>NativeArgumentType&lt;T&gt;</code> \u2014 sealed hierarchy wrapping Brigadier/Paper argument types (40 types)</li> <li><code>ComshopCustomArgumentType&lt;T, N&gt;</code> \u2014 user-defined converters over a native type</li> </ul>"},{"location":"internals/architecture/#runtime-loading","title":"Runtime loading","text":"<p><code>comshop-front</code> does not know the implementation at compile time:</p> <pre><code>object ComshopLoader {\n    var registrarClassPath: String = \"com.github.ityeri.comshop.impl.CommandRegistrarImpl\"\n\n    fun loadRegistrarImpl(): AbstractCommandRegistrar =\n        try {\n            Class.forName(registrarClassPath).getDeclaredConstructor().newInstance()\n                as AbstractCommandRegistrar\n        } catch (_: ClassNotFoundException) {\n            throw ClassNotFoundException(\n                \"The registrarClassPath is invalid or cannot find loader class. Is the comshop-impl dependency added?\"\n            )\n        }\n}\n\nobject CommandRegistrar : AbstractCommandRegistrar by ComshopLoader.loadRegistrarImpl()\n</code></pre> <p><code>AbstractCommandRegistrar</code> (in <code>comshop-interface</code>) is the SPI every implementation provides:</p> <pre><code>interface AbstractCommandRegistrar {\n    fun init(plugin: JavaPlugin)\n    fun register(node: Node&lt;ComshopCommandNode&gt;)\n}\n</code></pre>"},{"location":"internals/architecture/#the-conversion-pipeline-inside-an-impl-module","title":"The conversion pipeline (inside an impl module)","text":"<p><code>CommandRegistrarImpl</code> collects nodes via <code>register(node)</code>, then on Paper's <code>LifecycleEvents.COMMANDS</code> event converts and registers each one:</p> <ol> <li>toFinalBuilderBoundary \u2014 a single optics traversal (<code>nodePTraversal</code>, built with Arrow Optics <code>PTraversal</code>/<code>PLens</code> and a custom <code>Choice3</code>/<code>split3</code> combinator) maps every <code>ComshopCommandNode</code> in the tree directly to a <code>BrigadierBuilderBoundary</code> and connects them, chaining two per-node steps:</li> </ol> <pre><code>fun toFinalBuilderBoundary(node: Node&lt;ComshopCommandNode&gt;): BrigadierBuilderBoundary =\n    nodePTraversal&lt;ComshopCommandNode, BrigadierBuilderBoundary&gt;()\n        .modify(node) { node -&gt;\n            node\n                .toBrigadierFragment()\n                .toBrigadierBuilderBoundary()\n        }\n        .connectBuilderBoundaries()\n</code></pre> <ol> <li>toBrigadierFragment (BrigadierFragmentGenerator.kt) \u2014 per-node intermediate representation:</li> </ol> <pre><code>sealed class BrigadierFragment {\n    class NodeBuilderFragment(val builder: BrigadierNodeBuilder) : BrigadierFragment()\n    class ExecutionFragment(val command: Command&lt;CommandSourceStack&gt;) : BrigadierFragment()\n}\n</code></pre> <ol> <li>toBrigadierBuilderBoundary (BrigadierBuilderBoundaryWrapper.kt) \u2014 wraps each fragment into a <code>BrigadierBuilderBoundary</code>:</li> </ol> <pre><code>class BrigadierBuilderBoundary(\n    val entries: Collection&lt;BrigadierNodeBuilder&gt;,\n    val exits: Collection&lt;BrigadierNodeBuilder&gt;,\n    val pendingCommand: Command&lt;CommandSourceStack&gt;? = null\n)\n</code></pre> <ul> <li><code>NodeBuilderFragment</code> \u2192 a boundary with one entry/exit</li> <li> <p><code>ExecutionFragment</code> \u2192 an empty boundary carrying the command as <code>pendingCommand</code></p> </li> <li> <p>connectBuilderBoundaries (BrigadierBuilderBoundaryConnector.kt) \u2014 links boundaries into a Brigadier tree:</p> </li> <li> <p><code>SingleNode</code> \u2192 its boundary</p> </li> <li><code>UnionNode</code> \u2192 boundaries merged by flattening entries/exits, preserving a single pending command (execution) if one is present</li> <li><code>ChainNode</code> \u2192 boundaries connected with <code>connectBoundaryChain</code> / <code>connectNext</code> (exits of one feed the entries of the next; a trailing execution becomes the pending command)</li> </ul> <p>The result must reduce to exactly one entry \u2014 the command's root literal \u2014 which <code>CommandRegistrarImpl</code> verifies before registering.</p> <ol> <li>Argument conversion \u2014 <code>NativeArgumentType</code> \u2192 Brigadier <code>ArgumentType</code> via <code>nativeTypeToBrigadierArgumentType</code>. Paper types that resolve lazily (entity selectors, positions, ranges) are wrapped in <code>CustomNativeArgumentType</code>, a <code>CustomArgumentType.Converted</code> that resolves against the <code>CommandSourceStack</code> during parsing.</li> <li>Custom arguments \u2014 <code>ComshopCustomArgumentType</code> \u2192 Paper's <code>CustomArgumentType</code> wrapper, delegating parse/suggest to the user's implementation.</li> <li>Suggestions \u2014 comshop suggestion lambdas are converted to Brigadier <code>SuggestionProvider</code> (<code>toBrigadierSuggestionProvider</code>), with tooltips serialized via <code>MessageComponentSerializer</code>.</li> </ol>"},{"location":"internals/architecture/#exception-mapping","title":"Exception mapping","text":"<ul> <li><code>ComshopCommandException</code> (user-facing) \u2192 Brigadier <code>SimpleCommandExceptionType</code> \u2014 the message is shown to the sender</li> <li><code>CommandSyntaxException</code> thrown from user blocks \u2192 wrapped in <code>IllegalStateException</code> with a warning (it belongs to Brigadier, not comshop)</li> <li>A conversion result that does not reduce to exactly one root literal \u2192 <code>IllegalStateException</code> (an internal comshop pipeline error)</li> </ul>"},{"location":"internals/architecture/#adding-a-new-paper-version","title":"Adding a new Paper version","text":"<p>See Adding a Version.</p>"},{"location":"reference/argument-types/","title":"Argument Types Reference","text":"<p>comshop provides 40 built-in argument types through factory functions. All of them are available inside <code>arguments</code> blocks, <code>unions</code>, and <code>customArgument</code> builders.</p>"},{"location":"reference/argument-types/#primitives","title":"Primitives","text":"Factory Returns Notes <code>boolean()</code> <code>Boolean</code> <code>int(min, max)</code> <code>Int</code> Default range: <code>Int.MIN_VALUE</code>..<code>Int.MAX_VALUE</code> <code>float(min, max)</code> <code>Float</code> <code>double(min, max)</code> <code>Double</code> <code>long(min, max)</code> <code>Long</code>"},{"location":"reference/argument-types/#strings","title":"Strings","text":"Factory Returns Notes <code>string(type)</code> <code>String</code> <code>type: StringType</code> \u2014 <code>WORD</code>, <code>QUOTED</code>, or <code>GREEDY</code> <code>word()</code> <code>String</code> Single word (no spaces) <code>quotedString()</code> <code>String</code> Quoted string (spaces allowed inside quotes) <code>greedyString()</code> <code>String</code> Everything after the argument, including spaces"},{"location":"reference/argument-types/#entities-players","title":"Entities &amp; Players","text":"Factory Returns <code>entity()</code> <code>Entity</code> <code>entities()</code> <code>List&lt;Entity&gt;</code> <code>player()</code> <code>Player</code> <code>players()</code> <code>List&lt;Player&gt;</code> <code>playerProfiles()</code> <code>Collection&lt;PlayerProfile&gt;</code>"},{"location":"reference/argument-types/#positions-rotation","title":"Positions &amp; Rotation","text":"Factory Returns Notes <code>blockPosition()</code> <code>BlockPosition</code> Integer block coordinates <code>finePosition(centerIntegers)</code> <code>Location</code> <code>centerIntegers</code> defaults to <code>false</code> <code>rotation()</code> <code>Rotation</code>"},{"location":"reference/argument-types/#world-state","title":"World &amp; State","text":"Factory Returns <code>blockState()</code> <code>BlockState</code> <code>itemStack()</code> <code>ItemStack</code> <code>itemPredicate()</code> <code>ItemStackPredicate</code> <code>namedColor()</code> <code>NamedTextColor</code> <code>component()</code> <code>Component</code> <code>style()</code> <code>Style</code> <code>signedMessage()</code> <code>SignedMessageResolver</code> <code>scoreboardDisplaySlot()</code> <code>DisplaySlot</code> <code>namespacedKey()</code> <code>NamespacedKey</code> <code>key()</code> <code>Key</code> <code>integerRange()</code> <code>Range&lt;Int&gt;</code> <code>doubleRange()</code> <code>Range&lt;Double&gt;</code> <code>world()</code> <code>World</code> <code>gameMode()</code> <code>GameMode</code> <code>heightMap()</code> <code>HeightMap</code> <code>uuid()</code> <code>UUID</code> <code>objectiveCriteria()</code> <code>Criteria</code> <code>entityAnchor()</code> <code>LookAnchor</code> <code>time(minTime)</code> <code>Int</code> <code>templateMirror()</code> <code>Mirror</code> <code>templateRotation()</code> <code>StructureRotation</code>"},{"location":"reference/argument-types/#registry-types","title":"Registry Types","text":"Factory Returns <code>resource(registryKey)</code> <code>T</code> \u2014 the registry entry (<code>registryKey: RegistryKey&lt;T&gt;</code>) <code>resourceKey(registryKey)</code> <code>TypedKey&lt;T&gt;</code>"},{"location":"reference/argument-types/#usage","title":"Usage","text":"<pre><code>arguments {\n    \"count\" named int(0, 100).asArg\n    \"target\" named player().asArg\n    \"position\" named blockPosition().asArg\n    \"color\" named namedColor().asArg\n}\n</code></pre> <p>See Defining Arguments for the full syntax.</p>"}]}